{
  "name": "tree-directory",
  "version": "0.0.1",
  "description": "Print a directory structure in the tree format",
  "main": "index.js",
  "scripts": {
    "test": "npm run lint && tap test/*.js",
    "cov": "tap --cov test/*.js",
    "lint": "eslint *.js 'lib/**/*.js' test/*.js bin/*.js",
    "coveralls": "COVERALLS_REPO_TOKEN=REPO_TOKEN npm run cov"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zoubin/tree-directory.git"
  },
  "keywords": [
    "tree",
    "directory"
  ],
  "author": "zoubin",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/zoubin/tree-directory/issues"
  },
  "homepage": "https://github.com/zoubin/tree-directory#readme",
  "dependencies": {
    "getp": "^1.2.0",
    "globby": "^4.0.0",
    "treeify": "^1.0.1"
  },
  "devDependencies": {
    "eslint": "^2.0.0",
    "tap": "^5.0.0"
  }
}
